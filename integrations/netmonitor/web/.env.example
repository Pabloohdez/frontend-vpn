# Copiar a /opt/netmonitor/web/.env y usar la MISMA clave que NETMONITOR_API_KEY
# en el `.env` de fronted-vpn. Genera un valor aleatorio largo (no commitear).
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_KEY=CAMBIA_POR_CLAVE_LARGA_ALEATORIA
